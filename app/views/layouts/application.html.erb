<!DOCTYPE html>
<html>
  <head>
    <title>Projetinho</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="bg-gray-100 text-gray-900 min-h-screen flex flex-col">
    <header class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4 flex justify-between items-center">
        <h1 class="text-xl font-semibold text-gray-800">MeuApp</h1>
        <nav class="space-x-4">
          <% if user_signed_in? %>
            <span class="text-gray-600">Olá, <%= current_user.email %></span>
            <%= link_to 'Sair', destroy_user_session_path, method: :delete, class: "text-red-600 hover:underline" %>
          <% else %>
            <%= link_to 'Login', new_user_session_path, class: "text-blue-600 hover:underline" %>
            <%= link_to 'Registrar', new_user_registration_path, class: "text-blue-600 hover:underline" %>
          <% end %>
        </nav>
      </div>
    </header>

    <main class="flex-grow container mx-auto px-4 py-8">
      <%= yield %>
    </main>

    <footer class="bg-white shadow-sm mt-8">
      <div class="max-w-7xl mx-auto px-4 py-4 text-center text-gray-500 text-sm">
        &copy; <%= Time.current.year %> Abigos Dev. Todos os direitos reservados.
      </div>
    </footer>
  </body>
</html>
